<?xml version="1.0" encoding="UTF-8"?>
<project name="puppet" default="build">
    <property environment="env"/>

    <target name="build" description="Gib mir nen echo">
        <echo message="${env.GIT_BRANCH}"/>
        <echo message="${env.CUSTOM_STUFF}"/>
    </target>

</project>